import { Router } from 'express';
// import UserController from './UserController.js';
const router = Router();

// const userController = new UserController();

// router.get('/', userController.getAll);
// router.get('/:id', userController.get);

// router.post('/', userController.create);
// router.post('/bulk', userController.createAll);

// router.put('/:id', userController.replace);
// router.put('/bulk', userController.replaceAll);

// router.patch('/:id', userController.update);
// router.patch('/bulk', userController.updateAll);

// router.delete('/:id', userController.remove);
// router.delete('/bulk', userController.removeAll);

export default router;
